services:
  - type: web
    name: gestion-conges
    env: php
    buildCommand: composer install --no-dev && php artisan config:cache && touch database/database.sqlite && php artisan migrate --force
    startCommand: php artisan serve --host=0.0.0.0 --port=$PORT
